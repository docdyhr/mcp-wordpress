# CLAUDE.md

Project instructions live in [`AGENTS.md`](./AGENTS.md) (DOX hierarchical framework — read it and its Child DOX Index
before editing anything). This file exists only because Claude Code looks for `CLAUDE.md` specifically; keep it as a
pointer, not a second copy, to avoid drift.
